import MemoryCore from "../components/MemoryCore";

export default function Foued() {
  return (
    <div
      style={{
        background: "#000",
        minHeight: "100vh",
        padding: "40px",
      }}
    >
      <MemoryCore />
    </div>
  );
}
